using System;
using System.Collections.Generic;
using System.Text;

namespace ClassSubmissionassignment286
{
    static class StaticClass
    {
        public static int Math2(string userinput2)
        {
            // This Method takes a string and converts to an int
            int result = Convert.ToInt32(userinput2);
            return (result);
        }
    }
}
